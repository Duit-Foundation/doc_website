# DefaultLogger

:::danger Deprecated
The `DefaultLogger` class is marked as deprecated and will be removed in the next major release.

Use [LoggingCapabilityDelegate](/docs/api/dart_api/LoggingCapabilityDelegate) instead.
:::

Base implementation of the [DebugLogger](/docs/api/dart_api/DebugLogger) interface, logs messages to the console in debug mode.
