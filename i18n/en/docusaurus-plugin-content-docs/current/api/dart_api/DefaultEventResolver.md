# DefaultEventResolver

:::danger Deprecated
The `DefaultEventResolver` class is marked as deprecated and will be removed in the next major release.
:::

Base implementation of [EventResolver](/docs/api/dart_api/EventResolver). This class handles server-sent events and passes execution flow to [UIDriver](/docs/api/dart_api/UIDriver).
