# HttpTransportOptions

:::danger Deprecated
The `HttpTransportOptions` class is marked as deprecated and will be removed in the next major release.

Use [HttpTransportManager](/docs/api/dart_api/HttpTransportManager) instead.
:::

A class for configuring the built-in HTTP transport implementation.
