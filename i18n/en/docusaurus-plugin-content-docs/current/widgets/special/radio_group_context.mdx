import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# RadioGroupContext

| Type name | Supports Property Animations | Controlled by default | May contain associated action | Link |
| - | - | - | - | - |
| TypeName | &cross;| :heavy_check_mark: | :heavy_check_mark: | - |

The `RadioGroupContext` widget serves as a centralized handler and owner of the current state of a group of [`Radio`] widgets (/docs/widgets/flutter/radio)

:::info
The value stored by the widget can be used as a data source for actions.

[More](/docs/core_concepts/actions_events)
:::

## Usage


<Tabs groupId="lang">
  <TabItem value="go" label="Go" default>
    ```go
RadioGroupContext(
		&duit_attributes.RadioGroupContextAttributes[bool]{
			GroupValue: false,
		},
		"group_id",
		&duit_core.RemoteAction{
			Event: "/on_change",
			DependsOn: []*duit_core.ActionDependency{
				{
					Id: "group_id",
					Target: "group_value",
				},
			},
		},
		nil,  //or
		// Radio[bool, duit_color.ColorString](
		// 	&duit_attributes.RadioAttributes[bool, duit_color.ColorString]{
		// 		Value: false,
		// 		Toggleable: true,
		// 	},
		// 	"",
		// 	false,
		// ),
	)

    ```
  </TabItem>
</Tabs>
