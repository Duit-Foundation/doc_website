import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# AnimatedBuilder

| Type name | Supports Property Animations | Controlled by default | May contain associated action | Link |
| - | - | - | - | - |
| AnimatedBuilder | &cross; | :heavy_check_mark: | &cross; | [link](https://api.flutter.dev/flutter/widgets/AnimatedBuilder-class.html) |

<details>
<summary>Attributes (outdated models)</summary>

```dart
final class AnimatedBuilderAttributes
    implements DuitAttributes<AnimatedBuilderAttributes> {
  final List<DuitTweenDescription> tweenDescriptions;
  final String? persistentId;

  AnimatedBuilderAttributes({
    this.tweenDescriptions = const [],
    this.persistentId,
  });
```

</details>

## Usage


<Tabs groupId="lang">
  <TabItem value="go" label="Go" default>
    ```go
AnimatedBuilder(
		&duit_attributes.AnimatedBuilderAttributes{
			PersistentId: "1", //override it for use builder inside component layout
			Tweens: []any{
				duit_animations.Tween(
					"height", //animated property name
					0.0, //begin
					100.0, //end
					3000, //duratuin
					duit_animations.OnEnter, //trigger
					duit_animations.Forward, //method
					false, //repeat
					"", //linear curve by default
				),
			},
		},
		"", //id
		nil, //child
	)

    ```
  </TabItem>
</Tabs>
