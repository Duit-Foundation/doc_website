import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# RadioGroupContext

| Название типа| Поддерживает анимации свойств | Контроллируемый по умолчанию | Может содержать связанное действие | Ссылка |
| - | - | - | - | - |
| TypeName | &cross;| :heavy_check_mark: | :heavy_check_mark: | - |

Виджет `RadioGroupContext` служит в качестве централизованного обработчика и владельца текущего состояния группы виджетов [`Radio`](/docs/widgets/flutter/radio.mdx)

:::info
Значение, хранимое виджетом, может быть использовано в качестве источника данных для действий.

[Подробнее](docs/core_concepts/actions_events.md)
:::

## Пример использования


<Tabs groupId="lang">
  <TabItem value="go" label="Go" default>
    ```go
RadioGroupContext(
		&duit_attributes.RadioGroupContextAttributes[bool]{
			GroupValue: false,
		},
		"group_id",
		&duit_core.RemoteAction{
			Event: "/on_change",
			DependsOn: []*duit_core.ActionDependency{
				{
					Id: "group_id",
					Target: "group_value",
				},
			},
		},
		nil,  //or
		// Radio[bool, duit_color.ColorString](
		// 	&duit_attributes.RadioAttributes[bool, duit_color.ColorString]{
		// 		Value: false,
		// 		Toggleable: true,
		// 	},
		// 	"",
		// 	false,
		// ),
	)

    ```
  </TabItem>
</Tabs>
