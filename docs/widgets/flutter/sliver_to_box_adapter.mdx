import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# SliverToBoxAdapter

| Название типа| Поддерживает анимации свойств | Контроллируемый по умолчанию | Может содержать связанное действие | Ссылка |
| - | - | - | - | - |
| SliverToBoxAdapter | &cross; | &cross; | &cross; | [link](https://api.flutter.dev/flutter/widgets/SliverToBoxAdapter-class.html) |

## Атрибуты

```dart
// SliverToBoxAdapter не имеет дополнительных атрибутов
// Используется напрямую без специальных атрибутов
```

## Пример использования

<Tabs groupId="lang">
  <TabItem value="ts" label="TypeScript" default>
    ```typescript
    SliverToBoxAdapter({
        id: "id",
        attributes: {},
        controlled: false,
    },
    //child
);
    ```
  </TabItem>
  <TabItem value="go" label="Go">
    ```go
    SliverToBoxAdapter(
      nil, // нет атрибутов
      "id",
      false,
      //child,
    )
    ```
  </TabItem>
</Tabs> 