import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# SliverAnimatedOpacity

| Название типа| Поддерживает анимации свойств | Контроллируемый по умолчанию | Может содержать связанное действие | Ссылка |
| - | - | - | - | - |
| SliverAnimatedOpacity | &cross; | :heavy_check_mark: | &cross; | [link](https://api.flutter.dev/flutter/widgets/SliverAnimatedOpacity-class.html) |

## Атрибуты

```dart
final class SliverAnimatedOpacityAttributes extends ImplicitAnimatable
    implements DuitAttributes<SliverAnimatedOpacityAttributes>, DuitSliverProps {
  final double opacity;

  @override
  final bool needsBoxAdapter;

  const SliverAnimatedOpacityAttributes({
    required this.opacity,
    required this.needsBoxAdapter,
    required super.duration,
    required super.curve,
    required super.onEnd,
  });
```

## Пример использования

<Tabs groupId="lang">
  <TabItem value="ts" label="TypeScript" default>
    ```typescript
    SliverAnimatedOpacity({
        id: "id",
        attributes: {
            opacity: 0.5,
            needsBoxAdapter: false,
            duration: 300,
            curve: "easeInOut"
        },
        controlled: false,
    },
    //child
);
    ```
  </TabItem>
  <TabItem value="go" label="Go">
    ```go
    SliverAnimatedOpacity(
      &duit_attributes.SliverAnimatedOpacityAttributes{
        Opacity: 0.5,
        NeedsBoxAdapter: false,
        Duration: 300 * time.Millisecond,
        Curve: duit_animations.EaseInOut,
      },
      "id",
      false,
      //child,
    )
    ```
  </TabItem>
</Tabs> 