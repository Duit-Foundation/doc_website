import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# SliverFillViewport

| Название типа| Поддерживает анимации свойств | Контроллируемый по умолчанию | Может содержать связанное действие | Ссылка |
| - | - | - | - | - |
| SliverFillViewport | &cross; | &cross; | &cross; | [link](https://api.flutter.dev/flutter/widgets/SliverFillViewport-class.html) |

<details>
<summary>Атрибуты (устаревшие модели)</summary>

```dart
final class SliverFillViewportAttributes
    implements DuitAttributes<SliverFillViewportAttributes>, SliverChildDelegateProps {
  final double viewportFraction;
  final bool padEnds;
  @override
  final List<NonChildWidget>? childObjects;
  @override
  final bool isBuilderDelegate, addAutomaticKeepAlives, addRepaintBoundaries, addSemanticIndexes;
  @override
  final int? childCount;

  const SliverFillViewportAttributes({
    required this.viewportFraction,
    required this.padEnds,
    required this.isBuilderDelegate,
    required this.addAutomaticKeepAlives,
    required this.addRepaintBoundaries,
    required this.addSemanticIndexes,
    this.childObjects,
    this.childCount,
  });
```

</details>

## Пример использования


<Tabs groupId="lang">
  <TabItem value="go" label="Go" default>
    ```go
SliverFillViewport(
      &duit_attributes.SliverFillViewportAttributes{
        ViewportFraction: 1.0,
        PadEnds: true,
        IsBuilderDelegate: false,
        AddAutomaticKeepAlives: true,
        AddRepaintBoundaries: true,
        AddSemanticIndexes: true,
        ChildCount: 10,
      },
      "id",
      false,
      //delegate,
    )

    ```
  </TabItem>
</Tabs>
