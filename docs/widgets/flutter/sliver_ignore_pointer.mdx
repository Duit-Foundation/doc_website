import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# SliverIgnorePointer

| Название типа| Поддерживает анимации свойств | Контроллируемый по умолчанию | Может содержать связанное действие | Ссылка |
| - | - | - | - | - |
| SliverIgnorePointer | &cross; | &cross; | &cross; | [link](https://api.flutter.dev/flutter/widgets/SliverIgnorePointer-class.html) |

<details>
<summary>Атрибуты (устаревшие модели)</summary>

```dart
final class SliverIgnorePointerAttributes
    implements DuitAttributes<SliverIgnorePointerAttributes>, DuitSliverProps {
  final bool ignoring;

  @override
  final bool needsBoxAdapter;

  const SliverIgnorePointerAttributes({
    required this.ignoring,
    required this.needsBoxAdapter,
  });
```

</details>

## Пример использования


<Tabs groupId="lang">
  <TabItem value="go" label="Go" default>
    ```go
SliverIgnorePointer(
      &duit_attributes.SliverIgnorePointerAttributes{
        Ignoring: true,
        NeedsBoxAdapter: false,
      },
      "id",
      false,
      //child,
    )

    ```
  </TabItem>
</Tabs>
