import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# IgnorePointer

| Название типа| Поддерживает анимации свойств | Контроллируемый по умолчанию | Может содержать связанное действие | Ссылка |
| - | - | - | - | - |
| IgnorePointer | &cross;  | &cross; | &cross; | [link](https://api.flutter.dev/flutter/widgets/IgnorePointer-class.html) |

<details>
<summary>Атрибуты (устаревшие модели)</summary>

```dart
import 'package:duit_kernel/duit_kernel.dart';
import 'package:flutter_duit/src/utils/index.dart';

class IgnorePointerAttributes
    implements DuitAttributes<IgnorePointerAttributes> {
  final bool? ignoring;

  IgnorePointerAttributes({
    this.ignoring,
  });
```

</details>

## Пример использования

<Tabs groupId="lang">
  <TabItem value="ts" label="TypeScript" default>
    ```typescript
    IgnorePoiner(
        {
            id: "id",
            attributes: {
                ignoring: true,
            },
            controlled: false,
        }
        //child
    )
    ```
  </TabItem>
  <TabItem value="go" label="Go">
    ```go
	IgnorePointer(
		&duit_attributes.IgnorePointerAttributes{
			Ignoring: true,
		},
		"id",
		false,
		nil, //child
	)
    ```
  </TabItem>
</Tabs>

